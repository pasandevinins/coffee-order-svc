Modified pipeline code:

```yaml
{'success': True, 'data': 'name: pasan-ai-test_pr-default-prod_app_0e79008 Pipeline (Skyu Generated)\n\'on\':\n  pull_request:\n    types:\n      - opened\n      - synchronize\n  workflow_dispatch: {}\n  repository_dispatch:\n    types: pipeline_e373576d-ff40-4dff-8293-3045a412c5c3\nenv:\n  ENCODED_PIPELINE_SECRET: ${{secrets.COFFEEORDERSVC_PASANAITEST_PRDEFAULTPROD_APP_0E79008}}\n  COMMIT_ID: ${{github.event.client_payload.commitId || github.sha || github.run_id}}\n  BRANCH_NAME: >-\n    ${{github.event.client_payload.branchName || github.event.branch ||\n    github.ref_name}}\njobs:\n  code-scan:\n    name: Code Scan\n    runs-on: ubuntu-latest\n    permissions: write-all\n    steps:\n      - name: \'Run :: Checkout repository for Code Scan\'\n        uses: actions/checkout@v4\n      - name: \'Run :: Checkout Specific Commit\'\n        env:\n          COMMIT_ID: ${{ github.event.client_payload.commitId }}\n          BRANCH_NAME: ${{ github.event.client_payload.branchName }}\n        if: >-\n          ${{ github.event.client_payload.commitId != null ||\n          github.event.client_payload.branchName != null }}\n        run: |2-\n\n              echo "Starting